// import * as firebase from "firebase/app"
// import "firebase/storage";

// var storageRef = firebase.storage().ref("project-images");
// state
// photo :null

// type file lable to photo no placeholder

// this.storageRef.child(this.state.username).put(this.state.photo)
// .then(data => data.ref.getDownloadUre())
// .then(url => {
//     return saveProfile ({
//         username:this.state.username
//         about:this.state.aboutme
//         photoUrl: url
//     })
// })

// onChange = this.setState e.target.files[0]

